# DX7 Voice Browser — runtime dependencies.
#
# Pinned from the build venv (.buildvenv, Python 3.14.6). Regenerate with:
#     .\.buildvenv\Scripts\python.exe -m pip freeze
#
# Note: pywebview needs proxy_tools, which is published as an sdist only.
# It is pure Python (no compiler needed), so install with:
#     pip install --only-binary=pythonnet,cffi -r requirements.txt
# The --only-binary guard is really about pythonnet: it embeds the CPython
# C API, and a source build would need MSVC + the .NET SDK. pythonnet 3.1.0
# ships a cp314 wheel, so this resolves from wheels on Python 3.14.

# --- Web server ---
fastapi==0.138.0
starlette==1.6.0
uvicorn==0.49.0
h11==0.16.0
anyio==4.14.2
idna==3.19
click==8.5.0

# --- Pydantic (FastAPI models) ---
pydantic==2.13.5
pydantic_core==2.46.5
annotated-types==0.8.0
annotated-doc==0.0.5
typing_extensions==4.16.0
typing-inspection==0.4.4

# --- Desktop shell ---
pywebview==6.2.1
pythonnet==3.1.0
clr_loader==0.3.1
cffi==2.1.1
pycparser==3.0
proxy_tools==0.1.0
bottle==0.13.4
