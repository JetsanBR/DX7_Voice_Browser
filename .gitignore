# Database (auto-generated on first run, should not be committed)
voices.db

# Python virtual environments
venv/
.buildvenv/

# PyInstaller build output (dx7_voice_browser.spec IS tracked)
build/
build-console/
dist/
dist-console/

# Python cache
__pycache__/
*.pyc
*.pyo

# Test artifacts (fixtures now go to the temp dir, but a hard crash mid-run
# or an older checkout can still leave these behind)
test_scan_root/
test_bank_*.syx
test_extract_blocks.syx
.pytest_cache/

# Other common virtualenv names
.venv/
